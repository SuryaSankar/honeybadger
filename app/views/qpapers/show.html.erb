<% content_for :mathjax_scripts do %>
<%= render partial: 'partials/mathjax_include' %>
<% end %>

<% content_for :pagewise_heading do %>
<%= @qpaper.full_title %>&nbsp;&nbsp;<%= @qpaper.university_course.try(:full_course_name) %>
<% end %>


<% content_for :navbar_breadcrumbs do %>
	<ul class="nav">
	  <li><%= link_to root_path  do %>Home &nbsp;&nbsp;<i class="icon-double-angle-right"></i><%end %></li>
  <li><%= link_to branch_path(@qpaper.university_course.course.branch_id) do %><%= @qpaper.university_course.course.branch.name %> &nbsp;&nbsp;<i class="icon-double-angle-right"></i><% end %></li>
<li><%= link_to university_path(@qpaper.university_course.university_id) do %><%= @qpaper.university_course.university.name %> &nbsp;&nbsp;<i class="icon-double-angle-right"></i><% end %></li>
<li><%= link_to university_course_path(@qpaper.university_course_id) do %><%= @qpaper.university_course.full_course_name %> &nbsp;&nbsp;<i class="icon-double-angle-right"></i><% end %></li>
	</ul>
<% end %>



<table class="table table-condensed">
<thead>
<tr>
<th>Q.No.</th><th>Question</th><th>Marks</th><th>Solutions</th>
</tr>
</thead>
<tbody>
<% @qpaper.examquestions.order("qnumber ASC, subquestion_no ASC").includes(:question).each do |eq| %>

	  <tr>
		<td><%= eq.qnumber.to_s %><%= eq.subquestion_no.nil? ? "" : "."+eq.subquestion_no.to_s %><%= eq.subsubqno.nil? ? "" : "."+eq.subsubqno.to_s %></td>
		  <td><%= raw render_markdown eq.question.qtext %></td>
		  <td><%= eq.mark.to_s %></td>
		  <td><%= link_to question_path(eq.question_id), class: "btn btn-success" do %>
			<i class="icon-unlock"></i>
		<% end %></td>
	  </tr>
<% end %>
</tbody>
</table>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              qpapers_path, :class => 'btn'  %>
<% if current_user == @qpaper.user %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_qpaper_path(@qpaper), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              qpaper_path(@qpaper),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
<% end %>
</div>


