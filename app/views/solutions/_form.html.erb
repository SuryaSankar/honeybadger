<%= simple_form_for @solution, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :answer %>
  <%= f.input :votes %>
  <%= f.input :correct %>
  <%= f.input :user_id %>
  <%= f.input :question_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                solutions_path, :class => 'btn' %>
  </div>
<% end %>
