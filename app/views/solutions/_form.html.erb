<%= simple_form_for @solution, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :answer %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                session[:previous_url], :class => 'btn' %>
  </div>
<% end %>
